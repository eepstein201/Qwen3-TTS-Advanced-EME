# CUDA/Colab dependencies for Qwen3-TTS
# Install with: pip install -r requirements-cuda.txt
torch>=2.0
qwen-tts
transformers==4.57.3
flask
librosa
soundfile
numpy
pydub
requests
gradio
accelerate
bitsandbytes
scipy
pyrubberband
pySBD>=0.3.4
num2words>=0.5.13
click>=8.0
# Flash Attention 2 — optional, auto-detected at runtime.
# Only installs on Ampere+ GPUs (L4, A100, H100). Safe to skip on T4.
# Install separately if needed: pip install flash-attn --no-build-isolation
# flash-attn
