# Plan: Full-Repo Review Remediation (Qwen3-TTS)

**Status: DRAFT — exploration in progress (3 review agents running)**

## Context

The user requested a comprehensive repo review — issues, dead code, inaccurate information, missing test coverage/gaps, performance challenges — and a plan to resolve everything discovered. `main` is at `e1f3d6b` (post-PR #213, the #192 serialization series complete).

## Ground truth already verified (2026-08-20)

### Static gates — ALL GREEN
- `ruff check qwen3_tts tests` → All checks passed
- `mypy qwen3_tts/{core,server,interface}` → Success: no issues found in 53 source files
- `bandit -r qwen3_tts -c pyproject.toml -q` → 0 real findings; 7 WARNING lines about **stale `nosec` markers** (B104 no longer failing) in `interface/ui/_facade.py:122`, `interface/ui/shared.py:875-878` — LOW cleanup candidate

### GitHub registry (open)
- **#214** (new, 2026-08-20): consolidated #192 follow-ups — 5 independent items:
  1. Torch-side unlocked inference: `_auto_create_pt_from_wav` (`core/engine/voice_prompt.py:54-85`) runs create inference unlocked inside `/generate`'s pre-lock prompt load (torch backend only; same concurrency shape as the MLX pairs #211/#212/#213 fixed)
  2. `/unload-asr` race (liveness-only): `unload_asr_model` clears the ASR global without `_asr_lock`; unload between `/transcribe`'s pre-ensure and locked generate → multi-minute lazy load holding `inference_lock`
  3. `/load-model` in-flight dedup: retry during in-flight load double-allocates ~2.5 GB; guard on `state.models_loading` (409 or await)
  4. E2E queuing coverage: no wire-level e2e test for `/transcribe` + `/create-voice-prompt` queuing behind a real `/generate` (opt-in `-m e2e`; needs `TTS_DISABLE_RATE_LIMITING=1`)
  5. #201-detector field watch: monitor `_warn_if_cap_reached` hits in `.voice_server.log` — any hit now points outside the closed pair model
- **#193** (bug): clone output duration tracks the reference transcript (42.5 s output for a 23-char request vs 41.3 s reference); `trim_icl_echo` is inert by default because ASR is not loaded at startup. Suggested path: (1) ASR-transcribe an output to prove the echo, (2) decide default (on-demand ASR load vs. document+surface), (3) revisit the 50%-of-output trim cap when echo is ~95%
- **#112**: upstream watch (Qwen3-TTS/MLX/vLLM) — monitoring, not actionable code
- **11 open dependabot PRs** (#202–#209, #165, #161, #162): several are known conflict traps — #203 `transformers>=5.15.0` contradicts the deliberate `transformers<5` cap (PR #100 root cause: numba/LLVM duplicate registration); #205 gradio `>=6.24`; #165 pytest-asyncio `>=1.4.0`. Need triage, not blind merge.

### Known-open from memory (to verify against agent findings)
- 2026-08-03 audit residue: 13 MEDIUM + 4 LOW + M1–M4
- Open structural debt: oversized modules, unmerged branches
- 2026-08-14 leftovers: root-artifact cleanup, load-ETA wiring

## Review findings

### Agent 1 — server + engine (COMPLETE: 3 HIGH / 8 MEDIUM / 14 LOW; 0 CRITICAL)

**HIGH**
| # | Finding | Location |
|---|---------|----------|
| H1 | WS streaming join uses flat 90 s default instead of `_stream_thread_join_timeout(len(text), req.max_chunk_chars)` — `req.max_chunk_chars` is in scope at the call site. On `/ws`, consumer gives up at 90 s and releases `inference_lock` while `model.generate()` still running (the exact race the documented rule exists to prevent). Only the HTTP helper is tested; nothing pins the WS call site | `server/websocket.py:530` vs `app_generation.py:823-826` |
| H2 | Startup `_background_load` catches only `(ImportError, RuntimeError, OSError, ValueError, MemoryError)`; HF download errors (`HfHubHTTPError`, `RevisionNotFoundError` — realistic via a typo in the new `models.<type>.revision` knob) escape, kill the daemon thread, and `models_loaded.set()` (after the loop, no `finally`) never fires → server wedges in 503 forever with no `model_load_errors` entry. API path `handle_load_model` has a catch-all; startup path doesn't | `server/app_lifespan.py:618,636` |
| H3 | Post-lock cache hit `continue`s without appending a result when the backing file vanished (unload/clear raced) → batch response short one item at 200 → client `IndexError` at `resp.json()["results"][0]`. Pre-lock path falls through to generation correctly; line 334 should too | `server/app_generation.py:315-334` (client crash at `server/client/generator.py:265`) |

**MEDIUM**
| # | Finding | Location |
|---|---------|----------|
| M1 | `pending_requests` entry not removed if client disconnects while queued for `inference_lock` (no `finally`) → `/queue-status` + `X-Queue-Position` drift up permanently | `app_generation.py:713-724` |
| M2 | Torch-side unlocked inference = #214 item 1 confirmed: `load_voice_prompt` → `_auto_create_pt_from_wav` runs `create_voice_prompt` (real inference) + possibly unlocked `load_model("clone")`, called pre-lock by all 3 generation paths; inconsistent with `/create-voice-prompt` which locks the same call | `core/engine/voice_prompt.py:54-85,150-164`; call sites `app_generation.py:349,684`, `websocket.py:385` |
| M3 | `seed_lock_chunks` knob is **inert**: all three backend runners unconditionally re-seed per chunk; server always injects resolved seed. UI checkbox + request schema + cache-key component `slc=` advertise a difference that doesn't exist (different cache keys, identical audio). Test passes only because the inner runner is mocked | `core/engine/inference.py:1402` vs `:509,646,221`; `validation.py:53,463`; `ui/tabs_generation.py:116`; `tests/test_seed_lock_chunks.py:66-68` |
| M4 | `stats()` runs `handle_stats` inline on the event loop (every sibling uses `to_thread`) — first `/stats` lazily imports torch+mlx and blocks the loop (incl. public `/health`) for seconds. `list_models()` same, smaller | `server/app.py:634,642` → `app_models.py:73,102` |
| M5 | b64 encode/decode + temp-file chmod run directly in async handler; comment claims "off the event loop" but only `sf.write` is offloaded | `app_generation.py:490-497,550` |
| M6 | Hardcoded 60 s client timeouts in inference-bearing paths: UI `/load-asr` (`model_management.py:193` — cold whisper load takes minutes; spurious-fail → retry double-load) and `tts voice preview` → `POST /generate` `timeout=60` (`generate_interactive.py:149` — always spuriously times out when queued) | both documented-rule violations |
| M7 | `models.<type>.revision` silently ignored on MLX backend (torch-only consumer); dead setting on the default macOS backend | `core/engine/model_loader.py:448-469` |
| M8 | `/update-model-config` unloads all models without `unload_model_cleanup()` (unlike unload + failed-load-recovery paths) → torch CUDA/MPS caches not reclaimed | `app_models.py:329-331` (the slot-nulling loop in `handle_update_model_config`, defined at `:281` — ref corrected from the stale `:402-406` when this doc moved into the repo, backlog Step 1B) |

**LOW (cleanup batch)**: dead code with grep-proof — `_estimate_eta` + `eta_cache` + `cache.eta_ttl_seconds` knob (test-only surface, `app_lifespan.py:67-102`), `cleanup_pid` (`app_lifespan.py:675`), `load_audio` (`audio_processing.py:56`), `_HAS_SLOWAPI` (always True, `app.py:47`), `_MODE_STRATEGIES_TORCH` (`inference.py:40`), `limiter_token`/`limiter_ip`/`_get_token_key` (no route uses those strategies, `app.py:459-514`); write-only `chunk_count` + wrong comment (`app_generation.py:800-820`); cancelled batch returns 200 with `results: []` → client `IndexError` instead of "cancelled" (`app_generation.py:299-303`); raw `str(e)` leaks absolute paths on `FileNotFoundError` (`app_generation.py:354,687`, `websocket.py:390`) vs `_sanitize_error` convention; cache-file orphan on concurrent duplicate-key misses (`app_generation.py:511-531`); `vllm.enabled=false` + `backend="vllm"` → generic 500 instead of clear config error (`inference.py:74-79`); `security_headers` annotated `-> dict` returns `Response` (`app.py:393`); no-op `del model` try/except (`app_lifespan.py:654`); Metal-retry splice defaults `silence_gap_seconds=0.1` vs `0.0` everywhere else (`inference.py:1565` vs `:1445`); stale bandit `nosec` markers (7 warnings, `_facade.py:122`, `shared.py:875-878`).

**Known-open verification (agent 1)**
- `/unload-asr` race (#214-2): **OPEN** — `unload_asr_model` takes no lock at all (`asr.py:234-252`), `/unload-asr` wrapper doesn't consult `inference_lock` or `generation_state`; adjacent: `/unload-model`'s `unload_model_cleanup()` GCs under live inference of a *different* mode.
- `/load-model` dedup (#214-3): **OPEN** — `models_loading` is set but never read as a guard (only the `/models` display reads it); double ~2.5 GB allocation on retry; the documented `LOAD_MODEL_TIMEOUT_SEC` spurious-timeout-retry scenario is precisely the trigger.
- Clean areas: auth/CSWSH/traversal, startup-lock/token atomicity, lazy imports (zero violations), temp-file discipline, bounded caches, lock ordering (no inversion), config package.

### Agent 2 — interface + CLI/UI + docs accuracy (COMPLETE: 3 HIGH / 13 MEDIUM / 11 LOW; 0 CRITICAL)

**HIGH**
| # | Finding | Location |
|---|---------|----------|
| U1 | Stop-button arm path returns **3 values for 4 wired outputs** → runtime `ValueError` from Gradio (`validate_outputs`). Arm path (:673-678) and canceled path (:685) both 3-for-4; fast path (:671) and confirmed path (:688) are correct. Also :673 assigns `ConfirmButton.click`'s 4-tuple to one variable. Documented Stop/confirm flow crashes instead of arming; no test executes the closure, playwright e2e early-returns | `ui/generation.py:673-678,685,690-693` |
| U2 | Delete-Voice (:225) and Unload-Model (:392) confirms store the **whole 4-tuple into `gr.State`** (arity matches so Gradio accepts); second click calls `.get()` on a tuple → `AttributeError`. Both destructive confirms never reach their confirmed branch. Corroborating: e2e `test_09`/`test_10` click Unload once and assert unloaded — stale/impossible against current wiring | `ui/tabs_management.py:194,225,362,392` |
| U3 | `TTSClient.generate_streaming()` **ignores the terminal error frame** (`sample_rate == 0`): parses JSON error bytes as float32 and yields garbage + `sr=0` instead of raising. CLI path handles it (`generate_server.py:386-395`, lockstep-guarded); the public client API (README:303) doesn't. Duplicate parsers with divergent caps (200 MB vs 100 MB) | `server/client/generator.py:404-436` |

**MEDIUM (selected)**
- U4: `_prepare_cancel_confirmation` reads `chunk_total`/`eta_sec` — fields `/generation-status` deliberately omits → progress math fabricates ≥100 %, fast path almost never fires, ETA always "N/A" (`ui/generation.py:153-158` vs `app.py:591-608`)
- U5: `update_startup_defaults` replaces `models.<type>` dicts wholesale → saving Startup Defaults silently deletes `models.<type>.revision` (`ui/model_management.py:221-227`)
- U6: `/load-asr` `timeout=60` in UI (confirms M6); voice-preview `/generate` `timeout=60` + unconditional `.pt` suffix; REPL `/prompt` only accepts `.pt` names → always "not found" on MLX (`generate_interactive.py:131-149,559-565`)
- U7–U11 (DRY): streaming frame parser ×2 (divergence IS U3); mode→payload mapping ×2; generation defaults hardcoded ×3 (currently in sync, nothing keeps them so); path-validation block copy-pasted ≥6× (srt.py/dialogue.py verbatim ~30-line duplicates); argparse parallel CLI surface re-implementing `tts list/stats/history/config-edit` (`generate.py:382-502`, only reachable via `python -m`)
- U12 (dead code, grep-proven zero production callers): `cli/batch.py:41 load_batch_file`; `ui/shared.py:522 remove_history_row`; `components.py` poll helpers (= T14); **`wavesurfer_js.py:292 get_streaming_trigger_js` — legacy browser-streaming JS passing `config.auth_token` to the browser; security-relevant hygiene**; undocumented argparse `main()`s in `tools/model_cache.py`/`uninstall.py`/`healthcheck.py`; discarded `ProgressIndicator` construction (`model_management.py:183`); `validate_config` corrects `advanced.vllm_*` keys no code reads (effective keys are `vllm.*`)
- U13: file-size violations (= agent 3 list); CODEMAPS "heaviest modules" note itself stale
- U14: `tts voice create` discards `create_voice.main()` return code → exits 0 on failure (`cli_voice.py:103-107`)
- U15: `tts uninstall config` non-atomic config write, bypassing `save_config` (`tools/uninstall.py:193-194`)
- U16: `tts config` subprocess with no TimeoutExpired/missing-wizard handling → raw traceback (`cli_config.py:29-34`)

**LOW (selected)**: healthcheck `size_str` unbound ≥1 TB; `model_cache.py` duplicate Size columns + wrong epilog examples (`--unused 30d` vs int; `uninstall --models` vs subcommand); `ui/__init__` `_SUBMODULES` omits `components`; `create_voice.py` open/xdg-open timeout unhandled; `payload` shadowed in `generate_server.py:387`; REPL prompt says `~/Downloads/` but honors config; misleading upsample message phrasing.

**Docs drift table (16 rows; claim → reality)** — worst offenders:
1. `README.md:303`: `client.generate_streaming(..., output="stream.wav")` — **no `output` parameter exists** → user code raises `TypeError`
2. `README.md:328,89-90`: `advanced.vllm_gpu_memory_utilization` (0.7) — key absent from defaults, read by no code; effective key `vllm.gpu_memory_utilization` = 0.9
3. `README.md:71,339`: `trim_icl_echo` "inert/dormant, no caller passes a transcript" — stale: `_reference_text_from_prompt` auto-resolves on every clone path (the *remaining* inertness is ASR-not-loaded = issue #193)
4-8. `ARCHITECTURE.md:8-15,40`: wrong config defaults (`default_clone_prompt` is null not a file; `language` is "auto" not "English"; rate limits 20/3/15/1 vs actual 10/5/10/2 + global 120; `aliases` is `{}`); `ARCHITECTURE.md` has **no file-tree section** though CLAUDE.md:137 points to one
9. `CLAUDE.md:88`: "single choke-point for all HTTP calls" — client submodules make ~15 direct `self._session` calls; `core/config/runtime.py:308` calls `requests.get` directly
10. `CODEMAPS/architecture.md`: still lists `+ protocols` (removed #179)
11-12. CODEMAPS generated-date mismatch (08-10 vs 08-12); test counts 2163+/108 vs 3,071/166 (= F16)
13-16. ARCHITECTURE names two functions that exist nowhere; "constants in core/config.py" (it's a package); tool help-text CLI examples wrong; "heaviest modules" stale

**Verified-accurate (no action)**: all ~40 `tts` commands exist; both 22-row endpoint tables match routes exactly; `docs/CONFIG.md` defaults match (checker-gated); COMMANDS.md matches; gradio-6 gotcha compliance clean; lazy imports clean; no shell=True/os.system/secrets; destructive UI actions have containment checks.

### Verification pass (main agent, post-review)

All 8 top findings re-read in source and **CONFIRMED**: H1 (`websocket.py:530` no timeout arg), H2 (`app_lifespan.py:618,625-627,636`), H3 (`app_generation.py:313-334` — `continue` outside the file-exists guard), U1 (4 outputs wired vs 3 returned ×2 branches), U2 (4-tuple into `gr.State`; 4-tuple contract proven by `generation.py:681` unpack), U3 (no `sr==0` check in client loop), T2 (zero-assert bodies shown), T3 (guard scopes `_modules_on_disk()`). T3's exact glob lines re-confirm at implementation time.

### Agent 3 — test coverage gaps + performance (COMPLETE: 3 HIGH / 12 MEDIUM / 5 LOW; 0 CRITICAL)

Test inventory reality: **3,071 test functions across 166 root modules** (+3 `tests/evaluations/`, +6 `tests/security/`); BATCHES registers 159 + 10 allowlisted.

**Oversized modules (>800-line guideline)**
`core/engine/inference.py` 1769 · `server/app.py` 1007 · `interface/generate.py` 902 · `ui/shared.py` 879 · `server/app_generation.py` 875 — and `app_generation.py:3` still claims extraction keeps modules under 800.

**HIGH**
| # | Finding | Location |
|---|---------|----------|
| T1 | **Independently confirms H1** (WS flat 90 s join, no test pins the WS call site — only the HTTP helper is tested) | `websocket.py:530`; sole test `test_streaming_thread_lifecycle.py:140` is HTTP-only |
| T2 | Two security e2e tests contain **zero assertions** — `test_02_xss_prevention_in_text` and `test_06_template_injection_prevented` loop payloads, discard results, no `assert` anywhere. XSS/template-injection guarantees with false assurance | `tests/test_e2e_security_validation.py` |
| T3 | `tests/security/` subpackage (6 modules incl. the http_client choke-point SSRF suite `test_server_request.py`, `test_ssrf_callsites.py`, `test_path_injection.py`) is **invisible to the batch-coverage guard** — `_modules_on_disk` globs only root `test_*.py` + `tests/evaluations/`; never runs in `run_batches.py` gates | `tests/test_batches_coverage.py:88-95` |

**MEDIUM**
| # | Finding | Location |
|---|---------|----------|
| T4 | `/load-model` in-flight dedup absent in code (confirms #214-3): `models_loading` display-only, never a gate; concurrent same-type loads double-allocate ~2.5 GB | `app_models.py:221-228` |
| T5 | `/unload-model` 409 guard misses the queued window (request waiting on `inference_lock` hasn't set `active` yet) | `app_models.py:320-327` |
| T6 | Streaming clone-speed stretch runs pyrubberband/librosa per ~2 s chunk (~30 subprocess/phase-vocoder passes for a 60 s clone); batch path stretches once. Deliberate per test, but perf/seam-quality cost worth revisiting | `inference.py:1658-1670` → `audio_processing.py:237-256` |
| T7 | `_crossfade_chunks` O(N²) — `np.concatenate` per chunk recopies the whole buffer; ~15 GB memcpy at N≈100 chunks. List-accumulate + single concat = O(N·L) | `inference.py:888-914` |
| T8 | `/generate` encodes WAV twice (response + cache file); `Accept: audio/wav` path encodes→base64→decodes | `app_generation.py:490-498,546-555` |
| T9 | No e2e for `/ws`, live stream-error sentinel, `/generate-stream`, `/transcribe`, `/create-voice-prompt`, `/rename-prompt`, `/prompt-details`, `/update-startup-config`, `/queue-status`, ASR load/unload | e2e suite |
| T10–T12 | #214 items verified absent as tests: e2e inference_lock queuing (concurrent `/generate`+`/load-model`), unload-asr race test, load-model dedup test | — |
| T13 | `_auto_create_pt_from_wav` (torch-only, #214-1): only mock-tested — callers' fallback is asserted, the function's own behavior (transcript read, x_vector mode, save/cache, implicit multi-minute `load_model("clone")`) untested | `voice_prompt.py:54-83`; `test_voice_prompts.py:133,168` patch it out |
| T14 | Dead UI polling helpers `poll_model_loading_state`/`poll_model_load_progress` (0 production callers; ETA actually flows via `_badge_from_models_payload` + 5 s Timer); structured `ProgressIndicator(eta_s=…)` machinery test-only | `ui/components.py:191,441` |
| T15 | ~25 hollow REPL tests (call helper, assert nothing — crash-only smoke) in `test_generate_interactive_ext.py`; smaller clusters in `test_generate_helpers.py`, `test_fastapi_app_ext3.py:466-488` (immutability comment with no assertion) | — |

**LOW**: `GenerateRequest.max_chunk_chars` unbounded numeric (only one in file; feeds T1 sensitivity) `validation.py:51`; streaming path mutates `generation_state` without `generation_lock` (batch/WS/cancel all lock) `app_generation.py:727-736,834-846`; `/queue-status` counts only streaming waiters, ignores batch `request_queue` (also: no non-empty-queue test) `app.py:611-621`; `_validate_audio` double-runs on MLX batch path `inference.py:597,1307`; tracked generated artifact `.reports/codemap-diff.txt`.

**Docs drift (F16)**: CLAUDE.md claims "2163+ tests across 108 modules" — actual 3,071 across 166+9. CLAUDE.md is the declared primary context file.

**Endpoint→test matrix (condensed)**: well covered — `/ws` unit suite (50+ tests: auth/cancel/disconnect/Origin/OOM), validation negative paths, rename rollback, preview traversal, cancel, shutdown, icl-echo/clone-speed/cap-warning/sample-rate/x-vector engine guards, bounded caches (history cap 10, gen-cache eviction), `/stats` O(1), config mtime-cache. **Gaps**: T2/T3/T9/T10–T13 above + `/queue-status` non-empty.

**Known-open verification (agent 3)**
- Root-artifact cleanup — **STILL OPEN**: ~18 untracked debris files at repo root (mp3s, wavs, txts, session .md/.png, `test/`, `info/`, `txt_prompts/`, venvs, lock) + 1 tracked generated artifact; `.gitignore` enumerates the names (acknowledged-but-uncleaned)
- Load-ETA wiring — **PARTIAL**: badge ETA live via `/models` + Timer; structured `ProgressIndicator` path dead (T14)
- `_auto_create_pt_from_wav` — exists, torch-only, mock-tested only (T13)
- #192 e2e/test leftovers — all three verified absent (T10–T12)

## Drift review (2026-08-24, pre-implementation)

Re-verified against the working tree at `e1f3d6b` (unchanged since the plan was written; no tracked-file modifications). **Code drift: zero.** All 8 top findings re-confirmed at their cited lines — H1 (`websocket.py:530` passes no timeout; default resolves to `_STREAM_THREAD_JOIN_FLOOR_SEC` = **90.0**), H2 (`app_lifespan.py:618` narrow tuple-except, `:636` `set()` outside any `finally`), H3 (`:334` `continue` outside the `os.path.exists` guard), U1, U2, U3 (no `sample_rate == 0` check in the client loop), T2 (`test_02` body has **0** asserts), T3 (`grep -c "tests.security" run_batches.py` → 0). Static gates still green with **exactly** the same 7 stale-nosec warnings. All 11 dependabot PRs still open, identical set. #214/#193/#112 open. Test count is **3073 collected** / 166 root modules (plan said 3,071 — numeric only, no action).

**Three corrections folded into the phases below:**
1. **Phase 7c was factually wrong** about `.venv-*`/lock gitignore state — corrected in place.
2. **CLAUDE.md is at 298/300 lines**, so Phase 7b and Phase 2 item-5 cannot add lines without a trim first — corrected in place.
3. **Root-debris scope is ~22 items, not ~18** — enumerated in place.

**Phase 0a RED-gate shape (TDD honesty):** adding assertions to hollow tests will mostly land **straight on GREEN** — the code under test is likely already correct, so the new assertion documents behavior rather than exposing a bug. Do **not** manufacture a RED. The honest narrow RED gate is to prove each new assertion is *live*: temporarily invert it (or point it at a value the server does not actually produce), observe the failure, restore, observe the pass. Record both observations as the RED/GREEN evidence. Phase **0b** has a genuine RED (widening the glob fails `test_batches_coverage` with 6 unregistered modules before registration), and is the smaller PR.

Stale worktree `.claude/worktrees/test-coverage-audit` (`eb8fa8e`) has **no unmerged commits** (`git log main..worktree-test-coverage-audit` empty) → safe to remove in 7c.

### Found during implementation (pre-existing, newly tracked)

**P1 — `tests/evaluations/test_speaker_similarity.py` fails at COLLECTION, aborting the whole suite.** Discovered 2026-08-24 while running Phase 0a's gates.

```
ERROR tests/evaluations/test_speaker_similarity.py - RuntimeError: Could not load libtorchcodec.
OSError: Could not load this library: .../site-packages/torchcodec/libtorchcodec_core4.dylib
!!!!!!!!!!!!!!!!!!!! Interrupted: 1 error during collection !!!!!!!!!!!!!!!!!!!!
```

Proven pre-existing by stashing all Phase 0a changes and re-running the identical command — byte-identical error. **Severity is higher than it looks locally:** a *collection* error under `pytest tests/ -m "not e2e"` is `Interrupted`, so **zero** tests run — the entire local suite gate is unrunnable without `--ignore`. The module IS registered in `BATCHES` (`run_batches.py:235`, 600 s timeout for the WavLM load), so its batch is affected locally too. Not in any GitHub issue and was not in this plan.

**Scope correction (PR #215 evidence):** this is **local-env-only — CI is NOT affected.** The `coverage` job plus all five `test` matrix legs went green on `9dc1a5b`, so the broken `torchcodec` install is specific to the local `qwen3-tts-mlx` conda env (CI either lacks torchcodec entirely or resolves a working wheel). That lowers urgency from "CI-blocking" to "local gate unusable", but does NOT close it: the local full-suite run is the pre-push gate this repo depends on, and a dev who runs it without `--ignore` gets zero tests and a red herring. Prefer the `importorskip`-style guard over an env repin, since that fixes it for every machine.

Owner: **Phase 0** (test-truth infra — a gate that cannot run guards nothing, same rationale as 0a/0b). Likely the torch/torchvision/torchaudio partial-upgrade drift already in memory (`torch-env-partial-upgrade-drift`) reaching torchcodec; check the matched-triple first. Options: repair the env pin, or make the module skip cleanly on an unloadable torchcodec (`pytest.importorskip`-style guard) so a broken optional dep degrades to a skip instead of killing collection — the repo already prefers auto-skip for optional deps.

Interim: Phase 0a's suite evidence was gathered with `--ignore=tests/evaluations/test_speaker_similarity.py`; that flag is a workaround, not the fix, and must not become permanent.

**STATUS UPDATE (2026-08-29): no longer reproduces — do not start work here without re-confirming.** `pytest tests/evaluations/test_speaker_similarity.py --collect-only` collected **9 tests** cleanly, and the full `pytest tests/ -m "not e2e"` run for PR #228 completed at **2996 passed / 12 skipped** with **no** `--ignore` flag and no collection `Interrupted`. Caveat: that was under **pyenv `python`**, not the `qwen3-tts-mlx` conda env where the failure was originally found — and per the `e2e-interpreter-drift-gradio-version` memory, interpreter drift is exactly how this repo produces env-specific phantoms. Re-check in `conda run -n qwen3-tts-mlx` before either fixing or closing this. If it is genuinely env-local and now healed, the `importorskip`-style guard is still the preferred durable fix (a broken optional dep should degrade to a skip, never kill collection for everyone).

## Remediation plan

**Decisions (user, 2026-08-21):** scope = EVERYTHING (fresh findings + #214 + #193 + dependabot + artifact cleanup) · #193 = prove, then auto-load ASR · dead code = delete all grep-proven symbols.

**Execution shape:** series of small feature-branch PRs (repo convention — Claude branches/commits/pushes/opens PR, user merges). Branch names must start `fix/` or `feature/` (CI push allowlist skips `chore/`/`refactor/`). Each PR: TDD, new test modules registered in `BATCHES`, CLAUDE.md updated on significant change, no AI attribution. Local gates before push: ruff + CLAUDE.md ≤300 guard + owning batch; full `pytest -m "not e2e"` before handoff (CI coverage job discovers more than batches). Gate merge commands on verified-green CI.

### Phase 0 — Make the safety net real (test-truth infra)
- **0a** ✅ DONE (PR #215, squash-merged `871fc43`, 2026-08-24). Fix hollow tests: real assertions in `test_e2e_security_validation.py` test_02/test_06 (T2); ~25 no-assert REPL tests in `test_generate_interactive_ext.py` (T15); missing immutability assertions `test_fastapi_app_ext3.py:466-488`; non-empty `/queue-status` test. **RED gate = prove each assertion is live by inverting it** (see "Phase 0a RED-gate shape" above); expect GREEN-on-first-run and do not fake a failure. Evidence: `docs/testing/phase0a-hollow-tests.tdd.md`. Also surfaced pre-existing **P1** (`tests/evaluations/test_speaker_similarity.py` libtorchcodec collection failure) below.
- **0b** ✅ DONE (PR #216, squash-merged `0ddaad8`, 2026-08-24). Batch-guard blind spot: include `tests/security/` in `_modules_on_disk` glob (`tests/test_batches_coverage.py`) + register its 6 modules in `BATCHES` (T3). **Found during fix:** 3 of the 6 modules (`test_seed_bounds`, `test_voice_name_validation`, `test_play_audio`) are plain pytest-style classes that `unittest.TestLoader` silently collects as 0 tests — registering them into `BATCHES` naively would have been a second hollow fix. Only the 3 genuine `unittest.TestCase` modules (`test_path_injection`, `test_server_request`, `test_ssrf_callsites`) went into `BATCHES` batch 1; the other 3 went into `INTENTIONALLY_UNBATCHED` with the measured reason (they already run for real under pytest/CI coverage). Evidence: `docs/testing/phase0b-security-batch-guard.tdd.md`.

### Phase 1 — HIGH correctness (all main-agent-verified) — ✅ **COMPLETE**

> All landed 2026-08-29: 1a/1b/1c PR #217 (`e95f7ff`) · 1d PR #218 (`4e4e619`) · 1R1-1R6 PR #228 (`22e22b1`) · 1e PR #229 (`ddc4ea6`).
- **1a (H1/T1)** ✅ DONE (PR #217). `websocket.py:530`: pass `_stream_thread_join_timeout(len(text), req.max_chunk_chars)`; test pinning the WS call site (not just the helper). RED was `None != 375.0`.
- **1b (H2)** ✅ DONE (PR #217). `app_lifespan.py`: per-model catch-all `except Exception` (log + `_sanitize_error` + record in `model_load_errors`, mirroring `handle_load_model`) AND `finally: models_loaded.set()` so startup can never wedge in 503. **Plan's stated trigger was wrong** — `HfHubHTTPError`/`RevisionNotFoundError` both subclass `OSError` (verified via `__mro__`), so the old tuple already caught them; the reachable gap is library API drift (`AttributeError`/`TypeError`/`KeyError`) and the tests use those.
- **1c (H3+L19)** ✅ DONE (PR #217). `app_generation.py:313-334`: fall through to generation when cache entry's file vanished (restructure the `continue`); cancelled batch returns an explicit `cancelled: bool` marker (not silent `results: []`); client raises a clear error instead of `IndexError` at `generator.py:265`.

#### Phase 1 review follow-ups (found reviewing PR #217; shipped in PR #228)

> **CORRECTION (2026-08-29).** These were originally recorded as "DONE (PR #217), fixed on the same branch". That was **wrong**: PR #217 merged at `f71e0ed`, but the commit carrying 1R1-1R3 (`b782383`) was **never pushed** — `git branch -r --contains b782383` returned nothing. So #217 merged **without** them and 1R1/1R3 stayed live on `main` for five days. Caught 2026-08-29 by diffing the local branch against the post-merge `main` (`git diff main <branch> --stat` → 8 files, +368/−38) *before* deleting the branch as merged. **Lesson: after a squash merge, `git log main..<branch>` always looks non-empty (squash rewrites history) — it proves nothing. Use `git diff main <branch>`, which is empty iff the content really landed.** Rescued via cherry-pick onto the new `main` and merged as **PR #228 (`22e22b1`)**.

- **1R1** ✅ DONE (PR #228). The 1b `try/finally` opened too late — the function-local engine import and the `settings.get()` loop building `models_to_load` sat ABOVE it, so a broken native install (`ImportError`) or a config.json mapping a model to a bool (`AttributeError`) still wedged startup in permanent 503. Both reproduced on the branch head. `try:` now opens on the first statement of the body, with a top-level `except Exception` that logs the fatal setup error.
- **1R2** ✅ DONE (PR #228). The 1c client fix missed `generate_dialogue` — the sibling `resp.json()["results"][0]` in the SAME module (`generator.py:564`), with `cancel_generation()` as the next method in the class. Guard extracted to a module-level `_first_result(payload)` helper (DRY); both call sites route through it.
- **1R3** ✅ DONE (PR #228). `interface/generate_server.py::generate_via_server` guarded only a MISSING `results` key, never an empty/short list, and ignored the new `cancelled` flag. Its nine callers index `results[0]` (`cli/srt.py`, `cli/dialogue.py`, `generate_interactive.py`) or iterate it (`interface/generate.py` — **wrote fewer .wav files than requested, exit 0, no warning**). Now raises `TTSGenericError` when `len(results) != len(texts)`, naming both counts and cancellation as the cause.

#### Phase 1 deferred review findings — ✅ ALL DONE (PR #228, `22e22b1`, 2026-08-29)

> All three were LOW and none was a correctness defect, so each landed **GREEN on first run**. Per the Phase 0a convention the honest gate was proving each new assertion *live* by inverting the **production** side: 1R4 → drop `"cancelled"` from the uncancelled return (RED); 1R5 → delete the field from `GenerateResponse` (RED) **and** set `response_model_exclude_defaults=True` on `/generate` (RED, the exact named regression); 1R6 → add the rejected `None`→500 resolution (RED on the `None` case, `0` case still passing). One inversion was a **false negative worth remembering**: deleting `cancelled` at `validation.py:194` failed nothing because that line is `GenerationStatusResponse`, not `GenerateResponse` (`:143`) — always confirm which class an inverted line belongs to before reading a pass as proof. Evidence: `docs/testing/phase1-server-correctness.tdd.md`.
- **1R4** ✅ DONE (PR #228). `tests/test_batch_generation_state_ownership.py::TestBatchResultCompleteness::test_uncancelled_batch_reports_not_cancelled` asserts `assertFalse(result.get("cancelled"))`, which also passes when the key is absent entirely (confirmed: it is the 1 passing test in the 1c-server RED run). Tighten to `assertIn("cancelled", result)` + `assertIs(result["cancelled"], False)`.
- **1R5** ✅ DONE (PR #228). Nothing pinned `cancelled` on the **wire**. All three 1c server tests call `handle_generate` directly, bypassing `response_model`. `tests/test_response_contracts.py:188+` exists specifically to assert "every field must survive response_model filtering" but was only extended for per-result fields, not the new top-level flag. Serialization verified by hand (`{"results":[],"cancelled":true}`, schema props `['results','cancelled']`) — it works today, it is just unguarded against a future `response_model_exclude_defaults`. **Fix = one assertion in the existing contract test.**
- **1R6** ✅ DONE (PR #228) — **docstring corrected, code deliberately unchanged.** Resolving `None` through `_get_max_chunk_chars()` would *shorten* the join, and a join that is too short releases `inference_lock` while the model is still generating — the exact race it guards. Over-generous is the fail-safe direction, so the behaviour stays and the docstring now explains why; the test that made the same conflation (one subTest loop over `(0, None)`) was split into two named cases. Original finding: `app_generation.py::_stream_thread_join_timeout` docstring says "`0`/`None` disables chunking", but a *request-level* `max_chunk_chars=None` means "read from config" (default 500) per `inference.py:1354`, not "no chunking". The join then computes a whole-text timeout — over-generous, never too short, so fail-safe. Pre-existing in the helper (not introduced by Phase 1); correct the docstring and, if worth it, resolve `None` through `_get_max_chunk_chars()` so the bound matches reality.
- **1d (U1+U2)** ✅ DONE (PR #218, squash-merged `4e4e619`, 2026-08-29). UI confirm flows: correct the 4-tuple unpacking — `generation.py` arm (:673-678) and canceled (:685) branches return 4 values; `tabs_management.py` delete (:225) and unload (:392) handlers unpack `new_state, _, _, _ = btn.click(state)`. Closure-level tests that execute the wired handlers; fix stale playwright `test_09`/`test_10` (two-click flow). Browser-verify per memory (invoke handler directly as definitive check).
- **1e (U3+DRY-U7)** ✅ DONE (PR #229, squash-merged `ddc4ea6`, 2026-08-29). Rescued from an orphaned unpushed commit (`20abae1`, the second such find that day) and rebased onto `22e22b1` — replayed over #217/#218/#228 with **zero conflicts**, own stat byte-identical before/after (10 files, +626/−125). Each prior fix was re-verified present on the rebased tree rather than assumed, since a clean rebase can still drop the other side. Gates: ruff clean · mypy 54 files (was 53, new module) · bandit 0 · **3019 passed** / 12 skipped · batch-guard 3 passed · CLAUDE.md 298/300. **Merged with `docker-cpu-probe` still pending** (16 pass / 0 fail; user's explicit call — nothing in the diff touches Docker and `test-docker` passed). The real defect: `TTSClient.generate_streaming` never checked the error sentinel, so a server error's JSON payload went through `np.frombuffer` — at a length divisible by 4 it yielded garbage samples at `sr=0` and ended cleanly, making a failed generation indistinguishable from a successful one on the public client path (README:303). Original note follows. Commit **`20abae1`** ("fix(stream): one wire-format parser; TTSClient no longer yields error JSON as audio") sits on local branch `fix/phase1-stream-error-sentinel`, in the **locked** worktree `.claude/worktrees/phase1-high-correctness`. `git branch -r --contains 20abae1` → **empty**: never pushed, so no PR exists. It is real work — +626/−125 across 10 files, incl. a new `tests/test_stream_protocol.py` (205 lines) already registered in `run_batches.py`. **Rebase will conflict:** it edits `interface/generate_server.py`, `server/app_generation.py` and `server/client/generator.py` — the exact three files #217/#228 rewrote — and it predates all of #217/#218/#228. **Do NOT read `git diff main <branch>` as the work product here**: it reports −1786 lines only because the branch is behind `main` and lacks the test files those PRs added. Diff the *commit* (`git show --stat 20abae1`), not the branch, and rebase onto current `main` before judging it. Original scope: extract ONE streaming frame parser shared by `interface/generate_server.py` and `server/client/generator.py`: sentinel `sr==0` → raise `TTSGenericError` with decoded JSON; aligned chunk cap; lockstep test.

### Phase 2 — #214 concurrency completion
- **2a (item 1 / M2+T13)** ✅ DONE (PR #231, squash-merged `c0314a9`, 2026-08-31). Torch auto-create-from-`.wav` serialization: new `VoicePromptCreateRequired` control-flow exception (deliberately NOT in the `TTSError` hierarchy — worker-thread-raised, caught one frame later in `server/prompt_loading.py`, never user-facing); `_load_voice_prompt_torch(..., allow_create=True, clone_model=None)` raises it instead of creating inline; `_auto_create_pt_from_wav(..., model=None)` accepts a forwarded model so `load_model` is only called when none supplied. New `server/prompt_loading.py::load_voice_prompt_serialized` — unlocked `allow_create=False` probe → reuse `state.models["clone"]` or build `load_model("clone", warmup=False)` OUTSIDE the lock → re-enter `allow_create=True, clone_model=model` UNDER `inference_lock` as a leaf; drop-in contract preserved (FileNotFoundError/None unchanged) at all three call sites (`app_generation.py` ×2, `websocket.py`). The naive-fix trap (discarded pre-built model → weight construction re-runs INSIDE the lock) is avoided by `clone_model=` forwarding and pinned by mutation. Create-exactly-once convergence via `_torch_prompt_cache` populated before the second locked caller's top-of-function re-check (mutation-verified). 10 new tests (`test_issue214_prompt_create_serialization.py`, batch 3), 4 mutants killed, Reviewer A independently reproduced all mutants + gates. Santa dual review round 1: both PASS, 0 critical (Claude opus + gemini-3.1-pro-high); non-blocking findings landed in-branch (`f858698`). **CI lesson: torchless `.venv-310` is the CI proxy** — first push red'd every Tests/coverage leg because `TestEngineAllowCreateContract` reaches the real function-level `import torch`; gated with class-level `skipUnless(HAS_TORCH)` (mirrors `test_issue192_create_prompt_serialization.py:371`) in `f22b54b`; memory `torchless-venv-is-the-ci-proxy`. Gates: torchless 5 passed/5 skipped · torch env 10 passed · 3037 passed non-e2e (+10 vs baseline) · ruff · mypy 55 files · bandit 0 · batch 3 675 OK · CLAUDE.md 292/300 · CI fully green incl. `docker-cpu-probe` (sole failure = known GHAS `CAPIError: 400` infra job, non-blocking). Evidence: `docs/testing/phase2a-prompt-create-serialization.tdd.md` (with CI addendum). Deferred: `load_model` dedup for racing cold callers (feeds 2c's seam); fresh-build warm-up dropped (strictly safer than the old unlocked warm-up).
- **2b (item 2)** ✅ DONE (PR #230, squash-merged `a2a1748`, 2026-08-29). `/unload-asr` race: `unload_asr_model` takes `_asr_lock` (gc.collect() moved BEFORE `torch.cuda.empty_cache()`, both outside the lock — nulling a global only drops a reference) + `/unload-asr` route acquires `inference_lock` as a leaf (closes the identical check-then-use on the ICL echo-trim probe for free) + `handle_transcribe` re-checks `is_asr_loaded()` under the lock → retryable 503 `asr_unloaded` (`except HTTPException: raise` ahead of the catch-all keeps it from being rewrapped as `recovery="bug"`). Client: `UNLOAD_ASR_TIMEOUT_SEC = 900` + drift guard (same defect class as #211/#212). `voice_management.py` reads the **nested** `detail` payload (FastAPI serializes `HTTPException(detail={...})` as `{"detail": {...}}` — the old flat `.get("error")` was always `None`). New `tests/test_issue214_unload_asr_race.py` (batch 3); both key guards **mutation-verified** (old guards SURVIVED: call-counts and a substring check defeated by the route's own docstring; new guards KILLED: observed lock states `[False, True]` + AST-requires real `async with inference_lock`). `test_issue192_transcribe_serialization`'s `is_asr_loaded` stub made stateful (was permanently `False` → false-503 on the happy path); all its pre-existing assertions byte-identical vs `632553d`. Gates: 3028 passed / 12 skipped · ruff · mypy · bandit 0 · CLAUDE.md 298/300 · CI fully green incl. `docker-cpu-probe`. #214 reopened post-merge (keyword artifact had bulk-closed the 5-item umbrella; only item 2 done). Evidence: `docs/testing/phase2b-unload-asr-race.tdd.md`.
- **2c (item 3)** ✅ DONE (PR #233, squash-merged `3254cf6`, 2026-09-01; **T5 split out** — the queued-*generation* unload window needs the mode-aware claim absent from `app_generation.py:231`/`:745`/`websocket.py:473`; tracked with 2d). `/load-model` in-flight dedup: the write-only `models_loading` flag replaced by a per-load record table (`state.model_loads`, new `server/model_loading.py`). `claim_model_load` CAS under module-scope `MODEL_LOAD_LOCK` (fail-closed); duplicate → `ATTACH`, awaits the owner's `done` Event (870 s < client 900; retryable 503 `load_in_progress` on timeout; `is_disconnected()` probed ~2 s). Waiter classification from the record: FAILED+message mirrors the owner's `code`/`recovery`; FAILED-empty/CANCELLED → retryable 503. Release in the owner `finally`: identity-checked slot clear, explicit no-fall-through guards, **first-terminal-writer-wins** (a superseder's CANCELLED is never stomped by a stale owner's late FAILED). Epoch guard, both halves: `/update-model-config` + `/unload-model` bump `model_config_epoch` UNDER the claim lock; stale-epoch callers don't attach AND the owner re-checks at assign/error — supersede is LAZY, so a pre-bump owner finishing post-bump discards stale weights and answers 503, never 200. H4a: `/unload-model` during a load = structured 409. W1: warm-up failure KEEPS the model (startup precedent) — `warmup_failed` on owner+waiter responses, never `model_load_errors`. H2: `_background_load` WAITS on HTTP-owned records, records owner failures for /health, skips already-loaded models. `models_loading` deleted; `/models` loading wire field unchanged. Clients `load_model_on_server`/`toggle_model` unwrap via `_error_payload`. RED-vs-`c0314a9` reproduced the live double-build; **14 mutants killed** across three adversarial rounds (agy: 3 real findings; fastapi-reviewer PASS 0C: 2H/4M/3L fixed incl. lazy-supersede + loader fast-path; python-reviewer PASS 0C: 3M fixed incl. first-writer-wins). Gates: seven-module set 141 · batch 3 705 OK · 3034 passed / 17 pre-existing (stash-proven on `c0314a9`) non-e2e · ruff · mypy 56 files · bandit 0 · CLAUDE.md 293/300 · CI fully green (Tests 9m55s/10m20s; sole failure = known GHAS `CAPIError: 400` infra). **CI lessons (two red rounds on #233): (1) harness dimension — the batch runner + docker step run bare unittest, NO conftest, so state attrs the conftest initializes are absent there; handlers must tolerate bare states (lazy-init `model_loads`; the direct read red'd every matrix leg). Run the owning batch via the batch runner, not just pytest, before pushing. (2) cross-thread test races — a waiter thread started before the owner's claim was confirmed won the claim on fast CI machines; pin deterministic claim order.** Evidence: `docs/testing/phase2c-load-model-dedup.tdd.md`. Deferred: `_background_load` has no `_recover_from_failed_load` equivalent (pre-existing asymmetry, tracked follow-up); `/ready` residual — an HTTP-triggered cold load during startup holds 503 up to ~14.5 min (H2 wait), noted in ARCHITECTURE.md.
- **2d (item 4)** ✅ DONE (PR #235, squash-merged `d590572`, 2026-09-01). `tests/test_e2e_queueing.py` (4 opt-in tests: transcribe/create-prompt queued behind /generate, live /load-model coalescing, T5 choreography), module rate-limit preflight, INTENTIONALLY_UNBATCHED. Live run 3 PASS + 1 skip (pre-existing mlx-audio 0.4.8 create gap → issue #236).
- **Item 5** (#201 field watch) ✅ DONE (PR #235). ARCHITECTURE.md Cap-warning subsection (greppable string = message text, MLX-only, include `.log.1`); CLAUDE.md:218 net-zero clause; correction comment posted on #214.
- **T5 (2c split-out)** ✅ DONE (PR #235): /unload-model leaf lock + final-item in-lock generation_state reset + `_require_model_under_lock` 503 re-read (batch/streaming/ws) + /update-model-config lock + UNLOAD_MODEL_TIMEOUT_SEC=900 (3 callers). 16 unit tests, 5+ mutants, Gate A/B santa converged. **Phase 2 COMPLETE — #214 CLOSED.** New findings filed: #236 (mlx-audio create gap), #237 (pre-lock cancel erase), #238 (model_load_times leak).

### Phase 3 — UX/behavior bugs
- **3a (U4)** `_prepare_cancel_confirmation`: use only fields `/generation-status` actually returns (`chunk_index`, `elapsed_sec`) — honest elapsed-based progress; never fabricate ≥100 %.
- **3b (U5)** `update_startup_defaults`: immutable merge preserving per-model keys (`revision` etc.) instead of wholesale dict replacement.
- **3c (M6/U6+L17/L18)** Timeouts & MLX prompt naming: new `ASR_LOAD_TIMEOUT_SEC=900` (http_client.py, drift-guarded) for UI `/load-asr`; voice preview uses `_generation_timeout`; drop unconditional `.pt` suffix; REPL `/prompt` backend-aware.
- **3d (M3)** Make `seed_lock_chunks` real: when False, per-chunk derived seeds (`base_seed + chunk_index`) in all three runners; unmock `test_seed_lock_chunks` to pin inner seeding.
- **3e (server hygiene bundle, may split)** `/stats`+`/models` via `to_thread` (M4); `pending_requests` finally-removal (M1); `/update-model-config` calls `unload_model_cleanup` (M8); b64 encode/decode + tempfile off-loop (M5); `backend="vllm"` with vllm disabled → clear config error (L22); streaming `generation_state` under `generation_lock` (L18); `max_chunk_chars` Field bounds (L-17); sanitize `FileNotFoundError` detail paths (L20).
- **3f (M7)** `models.<type>.revision` on MLX: wire into `mlx_load_model` if supported, else startup validation warning + torch-only documentation.
- **3g (CLI/tools bundle)** `tts voice create` exit code (U14); `tts uninstall config` atomic write via `save_config` (U15); `tts config` subprocess TimeoutExpired/missing-wizard handling (U16); healthcheck ≥1 TB `size_str` (L19); cache/uninstall help-text fixes (L20b); `create_voice` open-timeout handling (L22b); `ui/__init__._SUBMODULES` + `components` (L21); REPL prompt path text (L24).

### Phase 4 — #193 (prove → auto-load ASR)
- **4a (prove)** ASR-transcribe a stored clone output (`lt1_24k` case: 23-char request → 42.5 s) confirming the reference transcript is spoken; post findings to #193.
- **4b (mitigate)** When `generation.trim_icl_echo` is true + clone mode + ASR not loaded → lazy-load ASR OUTSIDE `inference_lock` (mirroring the /load-model split), then proceed; surface the one-time load delay in CLI/UI status; rework the 50 %-of-output cap for ≥90 % echo cases (cap by echo-length estimate); CLAUDE.md + CONFIG.md updates; tests for the load-then-trim path.

### Phase 5 — Performance
- **5a (T7)** `_crossfade_chunks` O(N·L): list-accumulate + single `np.concatenate`; bench test at N≈100 chunks.
- **5b (T8)** Encode WAV once, reuse bytes for cache write; binary `Accept: audio/wav` path skips base64 round-trip.
- **5c (T6, measure-first)** Per-chunk clone-speed stretch (~30 pyrubberband passes per 60 s clone): prototype hoisting to end-of-stream or chunk-boundary batching; NO-GO with measurement doc is an acceptable outcome (repo precedent).
- **5d (T20)** Skip the redundant second `_validate_audio` pass on the MLX batch path.

### Phase 6 — Dead code deletion (all grep-proven) + DRY
- **6a (engine/server)** `_estimate_eta` + `eta_cache`/`eta_cache_lock` + `cache.eta_ttl_seconds` config knob (remove from defaults + CONFIG.md via checker) ; `cleanup_pid`; `load_audio`; `_HAS_SLOWAPI`; `_MODE_STRATEGIES_TORCH`; `limiter_token`/`limiter_ip`/`_get_token_key`; write-only `chunk_count`; no-op `del model` try/except; Metal-retry `silence_gap_seconds` 0.1→0.0 alignment; stale bandit `nosec` markers (`_facade.py:122`, `shared.py:875-878`).
- **6b (interface/tools)** `load_batch_file`; `remove_history_row`; `poll_model_loading_state`/`poll_model_load_progress` (+ orphaned `ProgressIndicator` ETA machinery — re-verify nothing shared is lost); **`get_streaming_trigger_js` (ships `config.auth_token` to the browser — delete first)**; undocumented argparse `main()`s in `tools/model_cache.py`/`uninstall.py`/`healthcheck.py`; discarded `ProgressIndicator` statement; `validate_config`'s dead `advanced.vllm_*` corrections.
- **6c (DRY)** shared `validate_output_path()` replacing ≥6 copy-pasted blocks (srt.py/dialogue.py verbatim); single mode→payload mapper; `DEFAULT_GENERATION_PARAMS` imported (not re-hardcoded ×3); retire the argparse info-command duplicates in `generate.py:382-502` (also shrinks it under 800).
- Every removal: re-grep proof at implementation time + test rewrite/deletion + BATCHES sync.

### Phase 7 — Docs truth pass
- **7a** README: fix `generate_streaming` example (no `output` param), vllm key names/defaults, `trim_icl_echo` staleness. ARCHITECTURE.md: correct config-defaults block (rows 4-8), remove nonexistent function names (row 13), `core/config` package refs (row 14), add the missing file-tree section or fix CLAUDE.md:137's pointer to it.
- **7b** CLAUDE.md: test counts (**3073 / 166+9**), choke-point wording (row 9 — honest scope or list tracked exceptions); regenerate `docs/CODEMAPS/` (clears rows 10-11,16) and remove tracked `.reports/codemap-diff.txt`.
  - **PREREQUISITE — line budget.** CLAUDE.md is at **298 lines** against the committed `claude-md-length-guard` hook's **≤300** ceiling: only 2 lines of headroom, and Phase 2 item-5 also wants to add there. Do the trim FIRST as its own step — relocate a long-form block (e.g. the `max_chunk_chars`/streaming-cap prose or the Testing section's E2E blockquotes) into `docs/00-Foundations/ARCHITECTURE.md` and leave a one-line pointer — then verify `wc -l CLAUDE.md` has real headroom before making any additive edit. Otherwise every 7b/2-item-5 push is blocked at the pre-push gate.
- **7c** Root-artifact cleanup: **~22 items, not ~18.** Delete: 3 `*.mp3` (ElevenLabs…, `ltaudio2.mp3`, `voicemail-…isolated.mp3`), 4 `*.wav` (`reference_audio.wav`, `streamed_sample_3462char.wav`, `test_LaLa.wav`, `test_LaLa_2.wav`), 3 `*.png` (`gradio-ui-loaded.png`, `ui-initial-state.png`, `ui-loaded.png`), 2 `ltref*.txt`, 4 session `*.md` (`current-tabs.md`, `current-ui-state.md`, `manage-voices-tab.md`, `manage-voices-tab-reloaded.md`), dirs `test/`, `info/`, `txt_prompts/`, plus `.DS_Store`, `.coverage`, and **5 log files** (`.voice_server.log`, `.voice_server.log.1`, `.voice_server.log.old`, `.tts_server.log`, `.tts_server_test.log`) — and the root `.worktrees/` dir, which holds only a `.DS_Store` (the plan previously named only `.claude/worktrees/`). Remove the stale `.claude/worktrees/test-coverage-audit/` worktree (verified 2026-08-24: **no unmerged commits**, safe).
  - **CORRECTION (2026-08-24):** `.venv-310`, `.venv-lock`, and `.voice_server.lock` are **NOT gitignored** — `git check-ignore -v` matches nothing and all three appear as `??` in `git status`. This step must **ADD** the `.gitignore` entries, not "confirm" them. Keep the files on disk (runtime); just stop them showing as untracked.

### Phase 8 — Dependabot triage (12 open PRs)

> **CORRECTION (2026-08-29): every PR number below is stale.** Dependabot superseded and re-opened the set; the old #203-#209/#161/#162 are closed. Re-verified open today: **#227** anthropic `>=1.0.0`, **#226** transformers `>=5.15.1`, **#225** gradio `>=6.25`, **#224** ruff `>=0.16.4`, **#223** uvicorn `>=0.52.4`, **#222** mlx-audio `>=0.5.0`, **#221** mlx `>=0.32.1`, **#220**/**#219** osv-scanner-action 2.5.1, **#207** mypy `>=2.3.1`, **#202** vllm `>=0.27.1`, **#165** pytest-asyncio `>=1.4.0`. The *dispositions* below still hold — map them onto the new numbers, and note the version targets moved too (anthropic is now a major `1.0.0`, not `0.122.0`, so it needs real evaluation rather than the old "dev-only, merge-safe" pass).
- **Merge-safe (verify CI actually ran first — `dependabot/*` branches get ZERO CI runs under the push allowlist; run local gates if not):** #204 ruff, #207 mypy, #206 uvicorn, #161/#162 osv-scanner actions, #208 anthropic (dev-only).
- **Evaluate:** #209 mlx-audio 0.4.8 (against #112 upstream watch + #192 repro harness), #165 pytest-asyncio 1.4 (strict-mode semantics), #205 gradio ≥6.24 (torch-env knot per memory — likely defer).
- **Close with rationale:** #203 transformers ≥5.15 (violates the deliberate `transformers<5` cap — PR #100 root cause), #202 vllm ≥0.27 (huge jump; defer to #112 watch).

### Phase 9 — Structural (optional, explicitly last)
- Split the 5 oversized modules — HIGH RISK: mock-patch seams (`@patch("<mod>.<name>")` must be grep-checked before every move) and CodeQL dismissals don't follow moved code. Natural shrinkage first: 6c retires ~120 lines from `generate.py`; `ui/shared.py` shrinks via the path-helper extraction. `inference.py` (1769) → defer with an ADR note unless separately commissioned.

### Explicit non-goals
- No behavior change to the three serialized #192 pairs (guarded by their test modules).
- No gradio/transformers/vllm major bumps (Phase 8 rationale).
- No `inference.py` split inside this program (Phase 9 caveat).

## Verification

Per PR: owning batch → `python -m pytest tests/ -m "not e2e"` → ruff/mypy/bandit → push → `gh pr create` → verify CI ran AND is green before handing over merge commands.
Program-level end-to-end (after Phases 1-2):
```bash
tts server stop && tts server start        # pick up changes
python -m pytest tests/ -m "not e2e"        # full non-e2e suite
TTS_DISABLE_RATE_LIMITING=1 tts server start && python -m pytest tests/ -m e2e   # e2e tier incl. new queuing tests
```
UI confirm flows: browser-verified via chrome-devtools MCP (click Stop/Delete/Unload through the two-step flow) PLUS direct handler invocation as the definitive check (memory: MCP clicks miss 5 s confirm windows).
Performance PRs (5a/5b/5c): before/after timing evidence in the PR body.
Phase 4a proof: ASR transcript excerpt posted to #193 before any mitigation merges.
